<![CDATA[
# API 参考

## `PlaceholderAPI.register(name, handler)`

注册一个新的占位符。

- `name` (String): 占位符的名称。
- `handler` (Function): 处理占位符的函数。

## `PlaceholderAPI.process(text)`

处理包含占位符的文本。

- `text` (String): 包含占位符的文本。
- 返回 (String): 处理后的文本。
]]>
